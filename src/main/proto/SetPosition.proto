syntax = "proto3";

package proto;

import "position.proto";

enum SetType {
    IMU = 0;
    ODOMETRY = 1;
}

message SetPosition {
  int64 timestamp = 1;
  Position3d new_position = 2;
  SetType set_type = 3;
}
